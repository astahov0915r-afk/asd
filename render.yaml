# https://render.com/docs/blueprint-spec
# В панели Render укажите Start Command: python server.py (не py — это только Windows).
services:
  - type: web
    name: electropick
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
